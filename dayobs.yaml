enabled: true
title: Date
description: >
  Demonstration of an DAYOBS parameter that defaults to the current date.
parameters:
  date:
    type: string
    format: dayobs
    dynamic_default: today
    description: >
      The DAYOBS (YYYYMMDD).
